# Two-Tier Flask Login App

## Run with Docker

1. Copy `.env.example` to `.env` and adjust if needed.
2. Build and start:

   ```bash
   docker-compose up --build
   ```

3. Open http://localhost:5000, register, then log in.
